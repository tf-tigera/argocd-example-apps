apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: guestbook-ingress
  namespace: guestbook
spec:
  order: 1002
  types:
    - Ingress
  ingress:
    - action: Allow
      destination:
        ports: 
        - 80
      protocol: TCP
